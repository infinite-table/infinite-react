{
  "name": "@infinite-table/examples",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 5555",
    "dev-no-output": "next dev &> /dev/null",
    "build": "rimraf .next && rimraf node_modules/@types/react && rimraf node_modules/react && NODE_ENV=production next build && NODE_ENV=production next export",
    "build-local": "NODE_ENV=production next build && NODE_ENV=production next export",
    "start": "next start",
    "test": "npm run build && npm run run-test",
    "test-local": "npm run build-local && npm run run-test",
    "run-test": "concurrently -s first --kill-others \"npm run start-server\" \"npm run play:test\"",
    "start-server": "http-server -p 5555 out -c300000",
    "play:test": "playwright test",
    "play:test-ui": "playwright test --ui",
    "xxxtest:debug": "PWDEBUG=console playwright test",
    "test:debug": "chokidar '**/*.spec.ts'  -c 'echo \"\n{path} was modified\n\" && npm run play:test -- --retries=0 {path} --debug'",
    "test:watch": "chokidar '**/*.spec.ts' '**/*.page.tsx' -c 'test_file_path=$(echo {path} | sed s/page.tsx/spec.ts/) && npm run play:test -- --retries=0 ${test_file_path}'",
    "ci:test": "concurrently -s first --kill-others \"npm run start-server -- -s\" \"npm run play:test\"",
    "tsc": "tsc --project tsconfig.json",
    "tscw": "tsc --watch --project tsconfig.json"
  },
  "dependencies": {
    "@radix-ui/react-checkbox": "^1.1.2",
    "@radix-ui/react-icons": "^1.3.0",
    "@vanilla-extract/css": "1.15.1",
    "ag-charts-react": "^9.1.1",
    "chokidar-cli": "^3.0.0",
    "isomorphic-fetch": "^3.0.0",
    "lucide-react": "^0.453.0",
    "next": "13.2.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-query": "^3.35.0",
    "sinon": "^10.0.0",
    "tailwind-merge": "^2.5.4",
    "tailwindcss-animate": "^1.0.7",
    "tinycolor2": "^1.4.2",
    "typescript": "4.6.2"
  },
  "devDependencies": {
    "@playwright/test": "1.32.1",
    "@tailwindcss/postcss": "^4.0.12",
    "@types/isomorphic-fetch": "0.0.35",
    "@types/lodash": "4.14.74",
    "@vanilla-extract/webpack-plugin": "^2.3.8",
    "babel-plugin-dev-expression": "^0.2.2",
    "class-variance-authority": "^0.7.0",
    "clsx": "^2.1.1",
    "concurrently": "^5.1.0",
    "http-server": "^14.1.1",
    "lodash": "^4.17.21",
    "normalize.css": "^8.0.1",
    "playwright": "1.32.1",
    "postcss": "^8.4.47",
    "react-select": "^5.4.0",
    "rimraf": "^3.0.2",
    "shadcn-ui-css-vars": "^2.0.0",
    "tailwindcss": "^4.0.12",
    "tslib": "^2.1.0"
  }
}
