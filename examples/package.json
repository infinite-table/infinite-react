{
  "name": "@infinite-table/examples",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "dev-no-output": "next dev &> /dev/null",
    "build": "rimraf .next && rimraf node_modules/@types/react && rimraf node_modules/react && next build && next export",
    "start": "next start",
    "test": "npm run build && npm run run-test",
    "run-test": "concurrently -s first --kill-others \"npm run start-server\" \"npm run play:test\"",
    "start-server": "http-server -p 3000 out",
    "play:test": "playwright test",
    "xxxtest:debug": "PWDEBUG=console playwright test",
    "test:debug": "chokidar '**/*.spec.ts'  -c 'echo \"\n{path} was modified\n\" && npm run play:test -- --retries=0 {path} --debug'",
    "test:watch": "chokidar '**/*.spec.ts'  -c 'echo \"\n{path} was modified\n\" && npm run play:test -- --retries=0 {path}'",
    "ci:test": "concurrently -s first --kill-others \"npm run start-server -- -s\" \"npm run play:test\"",
    "tsc": "tsc --project tsconfig.json",
    "tscw": "tsc --watch --project tsconfig.json"
  },
  "dependencies": {
    "@vanilla-extract/css": "^1.9.1",
    "chokidar-cli": "^3.0.0",
    "isomorphic-fetch": "^3.0.0",
    "next": "^12.3.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-query": "^3.35.0",
    "sinon": "^10.0.0",
    "tinycolor2": "^1.4.2",
    "typescript": "4.6.2"
  },
  "devDependencies": {
    "@playwright/test": "1.22.2",
    "@types/isomorphic-fetch": "0.0.35",
    "@types/lodash": "^4.14.172",
    "@vanilla-extract/next-plugin": "^2.1.1",
    "babel-plugin-dev-expression": "^0.2.2",
    "concurrently": "^5.1.0",
    "http-server": "^14.1.1",
    "lodash": "^4.17.21",
    "normalize.css": "^8.0.1",
    "playwright": "1.22.2",
    "react-select": "^5.4.0",
    "rimraf": "^3.0.2",
    "tslib": "^2.1.0"
  }
}
