React DataGrid columns should be easy to drag, but real apps also need saved views, resets, curated layouts, duplicate columns, and visibility-aware ordering.

New post: controlling column order in Infinite Table with `columnOrder` and `defaultColumnOrder`.
