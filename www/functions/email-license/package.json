{
  "name": "www-infinite-table-functions",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "install-license-generator": "npm run setup-npmrc && npm i @adaptabletools/infinite-license",
    "setup-npmrc": "echo //registry.adaptabletools.com/:_authToken=${PRIVATE_NPM_TOKEN} >> .npmrc && echo @adaptabletools:registry=https://registry.adaptabletools.com >> .npmrc && echo .npmrc"
  }
}
