import { internalProps } from '../../internalProps';

const { rootClassName } = internalProps;

export const TableRowClassName = `${rootClassName}Row`;
export const TableRowClassName__hover = `${TableRowClassName}--hover`;
