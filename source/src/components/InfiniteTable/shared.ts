import { columnHeaderHeightName } from './vars.css';
import { toCSSVarName } from './utils/toCSSVarName';

export const DEFAULT_ROW_HEIGHT = 40;
export const DEFAULT_COLUMN_HEADER_HEIGHT = toCSSVarName(
  columnHeaderHeightName,
);
